<UserControl x:Class="EasyCut.Shell.Views.VideoTaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="新建任务"
                    Command="{Binding NewTaskCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
            <Button Content="刷新"
                    Margin="8,0,0,0"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="*" />
                <DataGridTextColumn Header="进度" Binding="{Binding Progress}" Width="*" />
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedTime}" Width="*" />
                <DataGridTextColumn Header="最后更新" Binding="{Binding UpdatedTime}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>